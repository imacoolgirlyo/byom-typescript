import React from 'react';
import PageTemplate from '../components/base/PageTemplate';

const MainPage: React.FC = () => {
  return <PageTemplate>this is main page</PageTemplate>;
};

export default MainPage;
